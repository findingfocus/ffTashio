services:
  fftashio:
    build:
      context: .
      dockerfile: Dockerfile
    image: findingfocusdev/fftashio:latest
    container_name: fftashio
    restart: unless-stopped
    ports:
      - "3004:80"